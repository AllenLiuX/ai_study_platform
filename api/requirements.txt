fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.0
pydantic-settings>=2.5.2
python-dotenv>=1.0.1
httpx>=0.27.2
openai>=1.51.0
supabase>=2.8.0
PyJWT[crypto]>=2.9.0
python-multipart>=0.0.10
# Phase 1: 资料解析与切片
pypdf>=4.3.0
# Phase 4.2: Word 文档 (.docx) 解析
# .doc 是 OLE 二进制老格式,纯 Python 无良方案,后端会提示用户另存为 .docx
python-docx>=1.1.0
tiktoken>=0.7.0
# Phase 1.5: 平台公共资料生成 (读取课标 yaml)
PyYAML>=6.0
# Phase 4.1: 扫描版 PDF / 图片资料 → 视觉提取
#   pypdfium2 是纯 Python wheel,无需安装 poppler 等系统依赖
#   只用它把 PDF 每页渲染为 PNG,再喂给 OpenAI vision 提取文字 + 公式
#   Pillow 用来把 pypdfium2 的 bitmap 转 PNG 字节流
pypdfium2>=4.30.0
Pillow>=10.0.0
